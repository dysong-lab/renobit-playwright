name: Playwright Tests
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

# GitHub Pages 배포를 위해 필요한 권한 설정
permissions:
  contents: read
  pages: write
  id-token: write

# 여러 배포가 동시에 일어나지 않도록 큐(Queue) 처리
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: actions/setup-node@v6
      with:
        node-version: 22
    - name: Install dependencies
      run: npm ci
    - name: Install Playwright Browsers
      run: npx playwright install --with-deps
    - name: Run Playwright tests
      run: npx playwright test tests/basic/
      
    # 테스트가 실패하더라도 리포트는 보존하기 위해 !cancelled() 조건 사용
    - uses: actions/upload-artifact@v7
      if: ${{ !cancelled() }}
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 30
        
    # main 브랜치일 때만 Pages 배포용 아티팩트 생성 준비
    - name: Setup Pages
      if: github.ref == 'refs/heads/main' && !cancelled()
      uses: actions/configure-pages@v6
      
    - name: Upload artifact for GitHub Pages
      if: github.ref == 'refs/heads/main' && !cancelled()
      uses: actions/upload-pages-artifact@v4
      with:
        path: playwright-report/

  # GitHub Pages 배포 잡 (test 잡이 끝난 후 실행)
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: test
    # test가 성공하거나 실패하더라도 main 브랜치라면 무조건 실행하도록 always() 추가
    if: always() && github.ref == 'refs/heads/main' && (needs.test.result == 'success' || needs.test.result == 'failure')
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
